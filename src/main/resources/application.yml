server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: file:./config-repo
        default-label: main

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false